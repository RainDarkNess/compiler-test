# Многострочный комментарий

              , _ ,
             ( o o )
            /'` ' `'\
            |'''''''|
            |\\'''//|
               """

Конец многострочного комментария #

# Начало программы #
program var

# Объявление переменных #
a:int;
self:float;
z:int;
testBool:bool;
y:float;

# Объявление нескольких переменных #
test,test1,test2:float;

# Начало работы программы #
begin

# Присвоение int`овой величины #
;z assign 1

# Приравнивание к переменной float`овой величины #
;self assign 1.5
;y assign 1.5566999

# Использование научной записи #
;test assign 1.23E-1
;test1 assign 1.23E1
;test2 assign 1.23E+1

# 16тиричная запись #
;a assign 1ABFFFh

# Логическая запись #
;testBool assign false

# Перезаписывание значения переменной засчет другой #
;test assign test2

# Условный оператор #
if y GRT a then
   ;y assign 10.2 add y add a
   ;y assign z del testBool add 10
   ;z assign 2umn1add1disa2
;end

;y assign 10.5

# Цикл с известной длиной #
for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z

for i assign 1 val 10 do
   ;y assign 10 add z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
;displ y add 2, z disa 1, z add z


;displ y
end..