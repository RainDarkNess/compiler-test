# Многострочный комментарий

              , _ ,
             ( o o )
            /'` ' `'\
            |'''''''|
            |\\'''//|
               """

Конец многострочного комментария #

# Начало программы #
program var
a:int;
self:float;

# Объявление нескольких переменных #
test,test1,test2:float;
z:int;
testBool:bool;
y:float;

# Начало работы программы #
begin

# Приравнивание к переменной float`овой величины #
;self assign 1.5
;z assign 1

# Использование научной записи #
;test assign 1.23E-1
;test1 assign 1.23E1
;test2 assign 1.23E+1

# 16тиричная запись #
;a assign 1ABFFFh
;testBool assign false
;y assign 1.5566999
;test assign self

# Условный оператор #
if y GRT a then
   ;y assign 10.2 add y add a
   ;y assign z del testBool add 10
   ;z assign y umn y add z disa y
;end
;y assign 10.5

# Цикл с известной длиной #
for i assign 1 val 10 do
   ;y assign 10 add z

   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z disa 1
       while a LOWT 10 do
           ;a assign a add 1
       next
   next
next
end..