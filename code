# Многострочный комментарий

              , _ ,
             ( o o )
            /'` ' `'\
            |'''''''|
            |\\'''//|
               """

Конец многострочного комментария #

# Начало программы #
program var

# Объявление переменных #
a:int;
self:float;
z:int;
testBool:bool;
y:float;

# Объявление нескольких переменных #
test,test1,test2:int;

# Начало работы программы #
begin

# Присвоение int`овой величины #
;z assign 1

# Приравнивание к переменной float`овой величины #
;self assign 1.5
;y assign 1.5566999

# Использование научной записи #
;test assign 1.23E-1
;test1 assign 1.23E1
;test2 assign 123E2

# 16тиричная запись #
;a assign 1ABFFFh

# Логическая запись #
;testBool assign false

# Перезаписывание значения переменной засчет другой #
;test assign test2

# Условный оператор #
if y GRT a then
   ;y assign 10.2 | y | a
   ;y assign z / testBool | 10
   ;z assign 2 * 1 | 1 - 2
;end

;y assign 10.5

# Цикл с известной длиной #
for i assign 1 val 10 do
   ;y assign 10 | z
   # Цикл с неизвестной длиной #
   while z GRT z do
       ;z assign z - 1
       while a LOWT 10 do
           ;a assign a | 1
       next
   next
next
;displ y | 2, z - 1, z | z
;displ y
end..